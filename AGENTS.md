# Repository AI Agent Rules

## Git commits and pull requests

- Never add `Made-with: Cursor`, `Co-authored-by: Cursor`, or any similar Cursor attribution trailer to commit messages or pull request descriptions.
- Never pass `--trailer` flags to `git commit` unless the user explicitly asks for a specific trailer.
- Use plain `git commit -m "message"` without extra trailers.
